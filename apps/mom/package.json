{
  "name": "@openkrow/mom",
  "version": "0.1.0",
  "description": "Slack bot that delegates messages to the coding agent",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@openkrow/llm": "*",
    "@openkrow/agent": "*",
    "@slack/bolt": "^4.2.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
